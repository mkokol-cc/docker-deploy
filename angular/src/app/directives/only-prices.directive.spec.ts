import { OnlyPricesDirective } from './only-prices.directive';

describe('OnlyPricesDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyPricesDirective();
    expect(directive).toBeTruthy();
  });
});
